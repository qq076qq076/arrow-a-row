# M1-QA-001：核心灰盒回歸驗收

狀態：Completed（自動化）／Ready（實機）｜角色：測試人員｜Milestone：M1 核心灰盒｜完成日期：2026-07-28｜Commit：`測試(M1)：完成核心灰盒自動化驗收`（查詢本檔案 Git history）

## 測試版本與範圍

- 測試 commit：`6942a65 工程(M1)：完成核心灰盒玩法`。
- 範圍：M1-PM-001 的主選單、開始、單手指標輸入、第一個 Gate、HUD、灰盒 combat、build 與瀏覽器流程。
- 不在範圍：真機觸控、WebGL GPU profile、完整 60 秒死亡路徑、背景恢復、PWA 離線 Run、Boss／商店。

## 執行結果

| 類別 | 命令／環境 | 結果 |
| --- | --- | --- |
| Domain／內容／IndexedDB | `npm test` | Pass：4 files、5 tests。 |
| Typecheck／production build | `npm run build` | Pass。 |
| Chrome 手機 viewport | Playwright Pixel 7 | Pass：主選單 + 開始 Run + 左 Gate + HUD `2 箭`。 |
| WebKit 手機 viewport | Playwright iPhone 13 | Pass：主選單 + 開始 Run + 左 Gate + HUD `2 箭`。 |
| E2E 總計 | `npm run test:e2e` | Pass：4/4。 |

## M1 Gate 對照

| M1 標準 | 結果 | 證據 |
| --- | --- | --- |
| 10 秒內可拖曳並選第一 Gate | 自動化流程在約 3 秒完成 | Playwright 左 Gate case。 |
| 同組 Gate 不重複套用 | Pass | `M1RunSimulation.test.ts`。 |
| 自動射擊、敵人、HP、死亡、重開存在 | Pass（功能層） | `M1RunSimulation`、build 與程式 review；死亡／重開實機待測。 |
| 固定內容／輸入一致 | Pass | `M1RunSimulation.test.ts`。 |
| unit、build、Chrome／WebKit viewport | Pass | 本紀錄執行結果。 |

## 風險與後續驗收

1. Three.js initial chunk 693.36 kB（gzip 188.36 kB）有 Vite warning；在 M2 前驗收 lazy loading／bundle budget。
2. Playwright 不能取代 iOS Safari、Android Chrome 的實機觸控與 GPU 驗收；M2 Gate 前必補 AND-L、AND-S、IOS-N。
3. 目前 E2E 只驗證左 Gate；M2 要補右 Gate、敵人命中、死亡、重開、visibility checkpoint。

## QA 結論與下一步

M1 自動化品質 Gate 通過，可進入 M2 垂直切片。M2 開始前，PM 需安排 UI 高保真稿、CH01 GLB 灰盒資產、Boss／Reward／商店與 IndexedDB checkpoint 整合。每個 M2 角色工作包延續工作紀錄與中文 commit 規則。
