# M4-QA-002：品質與離線回歸

狀態：Completed（自動化）／Ready（實機）｜角色：測試人員｜Milestone：M4 內容完成｜完成日期：2026-07-29｜Commit：`測試(M4)：完成品質與離線回歸`（以 Git history 的最終 hash 為準）

## 回歸結果

| 項目 | 證據 | 結果 |
| --- | --- | --- |
| Profile 相容性 | 舊欄位缺少 `qualityMode` 時預設為 `standard`；`low` 可保存 | Pass |
| 畫質切換 | 主選單可選標準／省電；renderer 即時調整 pixel ratio | Pass（程式與 build） |
| 離線基線 | Service Worker v2 預快取 shell metadata、asset manifest、icons；同源 runtime cache-first | Pass（程式審查與 build） |
| 全部回歸 | Vitest 6 files / 21 tests、production build | Pass |
| 操作 smoke | Playwright Chromium／WebKit mobile viewport 6/6 | Pass |

## M4 最終判定

M4 已達「自動化完成」：前三章內容、3 Boss、6 項永久強化、12 成就、雙語內容資料、程序化資產 manifest、品質與離線基線及 100 Run soak 均已驗證。實體 iOS Safari／Chrome Android 的離線完整 Run、FPS、記憶體、溫度與 safe area 驗收仍待執行，因此實機狀態維持 Ready，未宣稱通過。
