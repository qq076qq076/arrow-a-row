# M5-ENG-021：移除玩家殘留模型

## 變更

- 玩家碰撞盒不再加入 Three.js render tree，避免舊模型幾何重新可見。
- 玩家模型掛載改以 `playerModelAnchor` 去重，並在重複載入時清除舊視覺子節點，只保留目前弓箭手模型。

## 驗證

- 第一關本地畫面只顯示單一弓箭手角色，未再出現舊人物頭部。
- `npm test -- --run`、`npm run build`、`npm run test:e2e` 通過。
