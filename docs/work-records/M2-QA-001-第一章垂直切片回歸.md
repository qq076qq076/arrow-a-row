# M2-QA-001：第一章垂直切片自動化回歸

狀態：Completed（自動化）／Ready（實機）｜角色：測試人員｜Milestone：M2 垂直切片｜完成日期：2026-07-28｜Commit：`測試(M2)：完成第一章垂直切片回歸`（以 Git history 的最終 hash 為準）

## 測試範圍

| 類型 | 案例 | 結果 |
| --- | --- | --- |
| Domain／Vitest | Gate 只套用一次、固定起始、Boss → Reward → complete、Reward 不可重複套用、Reward checkpoint 恢復 | 5 files / 9 tests 通過。 |
| Infrastructure／Vitest | Run checkpoint、Profile 金幣與永久等級 IndexedDB round-trip | 通過。 |
| Build | TypeScript `--noEmit`、Vite production build、Three/React code split | 通過；最大 chunk 為 Three 486.69 kB（gzip 122.72 kB）。 |
| E2E／Playwright | 主選單、開始第一章、左側第一個 Gate 增加箭數 | Chrome Pixel 7 與 WebKit iPhone 13，共 4/4 通過。 |

## M2 Gate 判定

| Gate | 判定 | 證據／備註 |
| --- | --- | --- |
| 新檔完整遊玩 CH01、擊殺 Boss | Pass（自動化 simulation） | Boss reward test 到達 `reward`。 |
| Boss 預警、Reward idempotent | Pass | Boss snapshot 預警／phase；同 Reward 第二次套用回傳 false。 |
| 金幣／商店／重啟一致 | Pass（repository + restore） | IndexedDB repository round-trip、checkpoint restore test。 |
| visibility/pagehide 保留同一 Run／Reward | Pass（程式與 unit coverage） | lifecycle save、啟動 CTA、restore 保留候選。 |
| Vitest/build/Chrome/WebKit | Pass | 執行紀錄如上。 |

## 尚待實機驗收（非自動化阻塞）

1. iOS Safari（實機）執行「加入主畫面」、背景化 30 秒、回前景與關閉後重新開啟 checkpoint。
2. Chrome Android（實機）確認 safe-area、單指拖曳、Boss 預警可讀性、IndexedDB 容量與 PWA 離線 shell。
3. 連續 10 局手動 soak，觀察 WebGL context、記憶體與觸控事件。

## 下一位角色交接：PM

M2 的自動化 Gate 已可簽核；依實機 QA 結果安排 M3 系統擴展、M4 正式美術／音訊與發佈準備。若目標是可交付 Web MVP，目前可將 M2 視為完成，實機驗收列為上線前 release gate。
