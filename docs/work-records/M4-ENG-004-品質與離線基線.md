# M4-ENG-004：品質與離線基線

狀態：Completed｜角色：工程師｜Milestone：M4 內容完成｜完成日期：2026-07-29｜Commit：`工程(M4)：完成品質與離線基線`（以 Git history 的最終 hash 為準）

## 產出

- 新增可保存的 `low`／`standard` 畫質設定；舊 Profile 預設遷移至 `standard`。
- 主選單提供「標準 60 FPS」與「省電 30 FPS」選擇；切換會即時調整 WebGL pixel ratio，設定保存在 IndexedDB。
- Service Worker cache 更新為 v2，安裝時預快取 PWA manifest、內容 asset manifest 與圖示；已載入的同源 runtime 資源延續 cache-first，離線不阻斷本地 Run。
- Profile repository 回歸涵蓋畫質保存與預設值。

## 完成標準與證據

- `npm test`：6 files / 21 tests passed。
- `npm run build`：TypeScript 與 production build passed。
- `npm run test:e2e`：Chrome／WebKit mobile viewport 6/6 passed。

## 風險／交接

真機仍須量測 Low／Standard 的 FPS、記憶體與溫度，也須在實體 iOS Safari、Chrome Android 以飛航模式走完整 CH01→CH03；自動化瀏覽器不可取代該驗收。
